import React from 'react';
import { link } from '@reach/router';

const Num4Page = props => {
    return (
        <div>
            <h1>The Number is: {props.id}</h1>
        </div>
    )
}

export default Num4Page;